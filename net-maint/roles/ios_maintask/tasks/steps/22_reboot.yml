---
- name: Reload IOS (may require confirm logic depending on platform)
  cisco.ios.ios_command:
    commands: ["reload"]
  register: rb

- name: Wait for IOS to return
  ansible.builtin.wait_for_connection:
    delay: 30
    timeout: "{{ reboot_timeout }}"
